Role: Senior Backend & AI EngineerObjective: Execute the "WeBot Pipeline Bug Fix Plan" for the hOMER CRM. This task involves fixing architecture issues, session management, and LLM loop logic.Tasks & Technical Specifications:1. Core Logic & Security (botPipeline.ts):Fix Session Key (Bug 7): Change the session document ID in getOrCreateSession from doc(phone) to doc(${agencyId}_${phone}) to support multi-tenancy.Fix Session TTL (Bug 2): Ensure getOrCreateSession returns the updated lastMessageAt value (the current timestamp now) instead of the old snapshot data. This is critical for the 24h expiry logic.Relax Property Routing (Bug 6): In handlePropertyRouting, add a guard clause. Do not route to a human agent if the message is shorter than 10 characters, even if it contains keywords like "רחוב", to allow the bot to handle short queries.2. Gemini Loop & RAG (handleWeBotReply.ts):Infinite Loop Protection (Bug 1): In the while (iterCount < 5) loop, after the loop finishes, add an explicit check: if (iterCount >= 5 && !finalReply). If true, set finalReply to a polite fallback: "אני מעבד את הנתונים עבורך, רק רגע..." to prevent empty responses.Global RAG (Bug 3): Enhance the property fetching logic. If lead.desiredCity exists, fetch up to 5 properties from cities/{city}/properties in parallel with the agency properties and include them in the Gemini context.3. Input Sanitization (sanitizeInput.ts):Preserve Structure (Bug 8): Modify the regex in sanitizeInput. Change .replace(/\s+/g, ' ') to .replace(/[ \t]+/g, ' '). This must preserve newlines (\n) while still collapsing multiple spaces/tabs, ensuring the LLM sees the original message structure.4. Code Cleanup (whatsapp.ts & index.ts):Remove Dead Webhook (Bug 4): Delete the whatsappWebhook function in whatsapp.ts and remove its export from index.ts.Fix Double Import (Bug 5): In whatsapp.ts, remove the dynamic require('./whatsappService') inside syncLeadChat and use the existing static import of syncChatHistory.Verification Requirements:Run npx tsc --noEmit after changes to ensure no TypeScript errors.Ensure all Firestore paths use the correct agencyId context.Maintain existing error handling and logging patterns.