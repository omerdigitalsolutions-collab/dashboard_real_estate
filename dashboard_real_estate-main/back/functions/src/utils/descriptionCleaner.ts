// Strip agent contact info from property descriptions

const FIRST_NAMES = new Set([
    "אבי","אביב","אביגיל","אברהם","אדם","אהוד","אהרון","אופיר","אור",
    "אורי","אורית","אורלי","אורן","אושר","איילה","איילת","איל","אילן",
    "אילנית","אירית","איתי","איתמר","איתן","אלאור","אלדד","אלה","אלון",
    "אלי","אליאב","אליהו","אלינור","אליעד","אליעזר","אלירן","אלכס",
    "אלעד","אמיר","אמירה","אמנון","אנדי","אסף","אסתר","אפי","אפרים",
    "אפרת","אראל","ארז","אריאל","אריה","ארנון","אשר",
    "בועז","בני","בנימין","ברוך","ברק",
    "גבי","גד","גדי","גדעון","גון","גיא","גיל","גילי","גלעד","גלר",
    "דביר","דבורה","דוד","דודי","דורון","דורית","דינה","דליה","דן","דנה",
    "דני","דניאל","דניאלה","דפנה","דקלה",
    "הגר","הדס","הדסה","הילה","הלל",
    "ויקי","ורד","ורדית",
    "זאב","זהבה","זהר","זיו",
    "חגי","חגית","חוה","חיה","חיים","חן","חנה","חנוך",
    "טל","טלי","טניה",
    "יאיר","יאנה","יגאל","יהודה","יהודית","יהונתן","יהושע","יואב","יואל",
    "יובל","יוכבד","יונה","יונית","יונתן","יוסי","יוסף","יורם","יעל",
    "יעקב","יפה","יפית","יצחק","ירון","ירדן","ישי","ישראל",
    "כוכבה","כפיר",
    "לאה","לבנה","ליאור","ליאורה","ליאל","ליאת","ליה","לילך","לימור",
    "לינוי","ליעד","ליר","לירון","לירז",
    "מאור","מאיה","מאיר","מוטי","מור","מורן","מיה","מיכאל","מיכל",
    "מירב","מירי","מנחם","משה","מתן",
    "נאור","נאוה","נגה","נדב","נהוראי","נוי","נועה","נועם","נופר",
    "נורית","ניב","ניסים","ניצן","ניר","נירית","נעמה","נעמי","נפתלי",
    "נתן","נתנאל",
    "סהר","סוזי","סוניה","סיגל","סיגלית","סימון","סיון","סמדר","סער",
    "עדי","עדן","עודד","עופר","עופרה","עומר","עידו","עידן","עינב",
    "עינת","עליזה","עמוס","עמיחי","עמית","ענבל","ענת","עפרה",
    "פיני","פנינה",
    "צבי","צביה","צבר","ציון","ציפי","ציפורה",
    "קובי","קורין","קרן",
    "רביד","רביב","רוי","רומי","רון","רוני","רונית","רונן","רותי",
    "רותם","רז","רחל","רינה","רינת","רן","רעות","רפאל","רפי",
    "שגיא","שולמית","שוש","שושנה","שחף","שחר","שי","שיר","שירה",
    "שירז","שלומי","שלומית","שלמה","שמואל","שמחה","שמעון","שני",
    "שקד","שרה","שרון","שרית",
    "תאיר","תהילה","תומר","תמי","תמר",
]);

const COMMON_WORDS = new Set([
    "את","של","על","עם","זה","זו","אל","גם","רק","כל","לא","כן",
    "הוא","היא","הם","הן","אני","אנחנו","שלי","שלו","שלה","שלנו",
    "או","אם","כי","אבל","אז","עוד","מאוד","יותר","פחות","בית",
    "דירה","דירת","חדרים","חדר","קומה","קומת","מטר","מרפסת","חניה",
    "חניון","מחסן","ממד","מעלית","שיפוץ","משופצת","משופץ","חדשה","חדש",
    "בלעדיות","למכירה","להשכרה","מיידי","גמיש","פינוי","כניסה",
    "בניין","בנין","פרויקט","פרוייקט","שכונת","שכונה","רחוב",
    "נוף","פתוח","שקט","שקטה","מרווח","מרווחת","גדול","גדולה",
    "קטן","קטנה","יפה","מהמם","מדהים","מדהימה","מושקעת","מושקע",
    "בוטיק","פינתי","פינתית","דיירים","שכנים","תושבים",
]);

const AGENT_SUFFIX_WORDS = new Set([
    "נדלן","נדל\"ן","מתווך","מתווכת","סוכן","סוכנת","יועץ","יועצת",
    "נכסים","מומחה","מומחית","השקעות","בשוהם","בתא","בתל","אביב",
    "והסביבה","ומרכז","בגוש","דן","בשרון","בירושלים",
]);

const CONTACT_KEYWORDS_RE = new RegExp(
    "(לפרטים|להתקשר|לתיאום|ליצירת\\s*קשר|צרו\\s*קשר|בשיחה" +
    "|וואוסאפ|וואטסאפ|ווטסאפ|whatsapp|טלפון|נייד|סלולר" +
    "|התקשרו|חייגו|שלחו\\s*הודעה|נדל\"?ן|תיווך|מתווכ[יםת]?|סוכן|יועץ)",
    "i"
);

function removePhones(text: string): string {
    const patterns = [
        /\+?972[\s\-.]?0?[2-9][\d\s\-.]{7,10}/g,
        /0[2-9][\d\s\-.]{7,10}(?=\s|$|[,;!?)])/g,
        /\*\d{4}/g,
        /1[-.‑]?800[-.‑]?\d{3}[-.‑]?\d{3,4}/g,
    ];
    for (const p of patterns) text = text.replace(p, '');
    text = text.replace(/0[2-9]\d[\d\s\-.]{6,9}/g, '');
    return text;
}

function removeContactLines(text: string): string {
    const lines = text.split('\n');
    const cleaned: string[] = [];
    for (const line of lines) {
        const stripped = line.trim();
        if (!stripped) continue;
        let phoneRemoved = stripped.replace(/0[2-9][\d\s\-.]{6,10}/g, '').trim();
        phoneRemoved = phoneRemoved.replace(/\+?972[\d\s\-.]{7,12}/g, '').trim();
        if (!phoneRemoved) continue;
        const match = CONTACT_KEYWORDS_RE.exec(stripped);
        if (match) {
            const before = stripped.slice(0, match.index).trim().replace(/[.,;:\-]+$/, '');
            if (before) cleaned.push(before);
            continue;
        }
        cleaned.push(line);
    }
    return cleaned.join('\n');
}

function removeAgentNames(text: string): string {
    const words = text.split(/\s+/);
    if (!words.length) return text;
    const TAIL_SIZE = 30;
    const head = words.length > TAIL_SIZE ? words.slice(0, -TAIL_SIZE) : [];
    const tail = words.length > TAIL_SIZE ? words.slice(-TAIL_SIZE) : words;
    const result: string[] = [];
    let skipNext = 0;
    for (let i = 0; i < tail.length; i++) {
        if (skipNext > 0) { skipNext--; continue; }
        const word = tail[i];
        const clean = word.replace(/[,.\-!?:;()"']/g, '');
        if (FIRST_NAMES.has(clean) && i + 1 < tail.length) {
            const nextClean = tail[i + 1].replace(/[,.\-!?:;()"']/g, '');
            if (
                nextClean.length >= 2 &&
                /^[֐-׿]+$/.test(nextClean) &&
                !COMMON_WORDS.has(nextClean) &&
                !/^ה.{1,3}$/.test(nextClean)
            ) {
                skipNext = 1;
                let j = i + 2;
                while (j < tail.length) {
                    const w = tail[j].replace(/[,.\-!?:;()"']/g, '');
                    if (AGENT_SUFFIX_WORDS.has(w)) { skipNext++; j++; } else break;
                }
                continue;
            }
        }
        result.push(word);
    }
    return [...head, ...result].join(' ');
}

export function cleanDescription(text: string): string {
    if (!text) return text;
    text = removeContactLines(text);
    text = removePhones(text);
    text = removeAgentNames(text);
    return text.replace(/[ \t]+/g, ' ').replace(/\n\s*\n+/g, '\n').trim();
}
