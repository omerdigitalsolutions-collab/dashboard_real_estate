# ─── WhatsApp Integration Environment Variables ───────────────────────────────
#
# Copy this file to .env (it is already git-ignored) and fill in the values.
#
# Set in Firebase with:
#   firebase functions:secrets:set WAHA_BASE_URL
#   firebase functions:secrets:set WAHA_MASTER_KEY
#   firebase functions:secrets:set WAHA_WEBHOOK_SECRET
#
# ─── WAHA Self-Hosted ─────────────────────────────────────────────────────────
# The base URL of your WAHA server (no trailing slash)
WAHA_BASE_URL=https://your-waha-server.example.com

# Master Bearer token for your WAHA server
# Leave EMPTY if using Green API instead
WAHA_MASTER_KEY=your_waha_master_key_here

# Secret header value to validate inbound webhooks
# Set the same value in your WAHA/Green API webhook settings
WAHA_WEBHOOK_SECRET=your_secret_token_here

# ─── Green API (alternative — single-tenant / per-agency API) ────────────────
# If using Green API instead of WAHA self-hosted:
# 1. Set WAHA_BASE_URL=https://api.greenapi.com
# 2. Leave WAHA_MASTER_KEY empty
# 3. Agency admins add their idInstance + apiTokenInstance via Firestore
#    (stored directly in agencies/{agencyId}/whatsappIntegration)
