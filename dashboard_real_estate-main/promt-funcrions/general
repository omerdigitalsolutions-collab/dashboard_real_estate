agencהנה הרשימה הסופית והמעודכנת של כל הפונקציות במערכת, בחלוקה לפי קולקשנים. הרשימה הזו משקפת את הארכיטקטורה המאובטחת והסופית שבנינו, לאחר העברת הפעולות הרגישות לשרת.

זוהי למעשה "מפת הדרכים" (Blueprint) של כל המידע שזורם באפליקציה שלך:

### 🏢 1. קולקשן `agencies` (משרדים והגדרות)

* **`createAgencyAccount` [צד שרת]:** יצירת סוכנות חדשה ומשתמש Admin ראשון (עוקף חוקי אבטחה כיוון שלמשתמש טרם הוגדרו הרשאות).
* **`updateAgencyGoals` [צד לקוח]:** עדכון יעדי המשרד החודשיים (מוגן בחוקי Firestore שדורשים הרשאת Admin).
* **`getLiveAgencyData` [צד לקוח]:** האזנה חיה (`onSnapshot`) לנתוני המשרד לטובת חישובי הדשבורד.

### 👥 2. קולקשן `users` (צוות והרשאות)

* **`inviteAgent` [צד שרת]:** יצירת מסמך "הכנה" (Stub) לסוכן ושליחת מייל/ווטסאפ עם לינק הזמנה.
* **`getInviteInfo` [צד שרת]:** פונקציה פתוחה (Public) שמושכת את שם המשרד למסך ההתחברות הראשוני של הסוכן.
* **`completeAgentSetup` [צד שרת]:** קישור ה-UID של חשבון הגוגל למסמך ה-Stub שהוכן מראש.
* **`updateAgentRole` [צד שרת]:** שינוי תפקיד מסוכן למנהל (הועבר לשרת מטעמי אבטחה מחמירים).
* **`toggleAgentStatus` [צד שרת]:** השעיה או הפעלה מחדש של סוכן (הועבר לשרת למניעת זיוף הרשאות).

### 🏠 3. קולקשן `properties` (ניהול מלאי ונכסים)

* **`addProperty` [צד לקוח]:** הוספת דירה חדשה מתוך טופס בממשק (מוגן בולידציית שדות ב-Rules).
* **`updateProperty` [צד לקוח]:** עריכת פרטי נכס קיים (מחיר, סטטוס, תיאור).
* **`deleteProperty` [צד לקוח]:** מחיקת נכס (מורשה ל-Admin בלבד דרך ה-Rules).
* **`getLiveProperties` [צד לקוח]:** האזנה חיה לכל הנכסים ששייכים ל-`agencyId` של הסוכן.

### 🎯 4. קולקשן `leads` (ניהול לידים)

* **`webhookReceiveLead` [צד שרת]:** נקודת קצה (Endpoint) לקליטת לידים אוטומטית מקמפיינים (Facebook/Make.com).
* **`addLead` [צד לקוח]:** הוספה ידנית של מתעניין (למשל משיחה נכנסת).
* **`updateLead` [צד לקוח]:** עדכון סטטוס ליד (חם, בטיפול) ותקציב.
* **`matchProperties` [צד לקוח]:** פונקציית סינון פנימית שרצה בדפדפן ומתאימה בין דרישות הליד למלאי הקיים.

### 🤝 5. קולקשן `deals` (צנרת עסקאות)

* **`addDeal` [צד לקוח]:** פתיחת עסקה חדשה המקשרת בין נכס לליד.
* **`updateDealStage` [צד לקוח]:** עדכון שלב בעסקה (גרירה בלוח הקנבן) - משפיע ישירות על חישוב העמלות הצפויות.
* **`deleteDeal` [צד לקוח]:** מחיקה או ארכוב של עסקה.

### 📋 6. קולקשן `tasks` (משימות יומיומיות)

* **`addTask` [צד לקוח]:** יצירת משימה או תזכורת.
* **`toggleTaskCompletion` [צד לקוח]:** סימון משימה כבוצעה או פתוחה.
* **`deleteTask` [צד לקוח]:** מחיקת המשימה מהרשימה.

### 🌐 7. קולקשן `shared_catalogs` (מיני-סייטים/Webot)

* **`generateCatalog` [צד שרת]:** יצירת טוקן קטלוג מאובטח ושליחת הודעת WhatsApp ללקוח עם הלינק (שימוש ב-API Key שמוסתר בשרת).
* **`getCatalogWithQueries` [צד לקוח]:** שליפת נתוני הקטלוג הספציפיים לתצוגה הציבורית של הלקוח קצה.

### 🔔 8. קולקשן `alerts` (התראות מערכת)

* **`triggerSystemAlert` [צד שרת - מופעל אוטומטית]:** טריגר רקע (`onDocumentUpdated`) המזהה עסקה שנסגרה ומייצר התראה לכל צוות המשרד.
* **`markAlertAsRead` [צד לקוח]:** סימון התראה כ"נקראה". מבוצע מהלקוח לחוויית משתמש מהירה, ומוגן באמצעות חוק אבטחה ספציפי שמאפשר לעדכן אך ורק את השדה `isRead` לערך `true`.